# توضیحات کامل رفع مشکل Timeout

## 🔴 مشکل چیست؟

خطای `Maximum execution time of 30 seconds exceeded` به این معنی است که:
- اجرای کد بیش از 30 ثانیه طول کشیده
- PHP به صورت پیش‌فرض timeout 30 ثانیه‌ای دارد
- سرور شما احتمالاً بدون تنظیم timeout افزایش‌یافته اجرا شده

## ✅ چه کاری انجام دادم؟

### 1. به‌روزرسانی `package.json`
پورت را از 6012 به 7668 تغییر دادم و `max_execution_time=300` اضافه کردم:
```json
"serve": "php -d max_execution_time=300 artisan serve --host 0.0.0.0 --port 7668"
```

### 2. افزودن Timeout در `AppServiceProvider`
به عنوان راه‌حل پشتیبان، timeout را در ServiceProvider تنظیم کردم تا اگر دستور command line کار نکرد، این تنظیم فعال شود.

## 📋 مراحل بعدی برای شما

### مرحله 1: توقف سرور فعلی
در ترمینالی که سرور Laravel در حال اجرا است:
- کلید `Ctrl + C` را بزنید تا سرور متوقف شود

### مرحله 2: پاک کردن Cache
```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

### مرحله 3: راه‌اندازی مجدد سرور
یکی از این دو روش را استفاده کنید:

**روش 1: فقط Laravel**
```bash
npm run serve
```

**روش 2: Laravel + Vite (توصیه می‌شود)**
```bash
npm run start
```

### مرحله 4: تست کردن
به `http://127.0.0.1:7668/admin` بروید و بررسی کنید که:
- ✅ صفحه Dashboard لود می‌شود
- ✅ خطای timeout برطرف شده
- ✅ Widget ها نمایش داده می‌شوند

## 🔍 اگر مشکل باقی ماند

### بررسی 1: مطمئن شوید سرور با npm run serve اجرا شده
- اگر سرور را با دستور دیگری اجرا کرده‌اید (مثلاً `php artisan serve` مستقیم)، آن را متوقف کرده و `npm run serve` استفاده کنید

### بررسی 2: لاگ‌ها را چک کنید
```bash
# لاگ Laravel
tail -f storage/logs/laravel.log
```

### بررسی 3: Widget را موقتاً غیرفعال کنید
اگر فکر می‌کنید مشکل از Widget است، در `app/Filament/Admin/Pages/Dashboard.php`:
```php
protected function getWidgets(): array
{
    return []; // موقتاً خالی بگذارید
}
```

## ⚙️ توضیحات تکنیکی

### چرا timeout افزایش دادیم؟
- در development، کامپایل اولیه Blade templates ممکن است زمان‌بر باشد
- Widget ها ممکن است در اولین بار نیاز به initialization داشته باشند
- 300 ثانیه (5 دقیقه) برای development مناسب است

### چرا دو جا تنظیم کردیم؟
1. **Command line (`-d max_execution_time=300`)**: بهترین روش، قبل از اجرای PHP اعمال می‌شود
2. **AppServiceProvider**: راه‌حل پشتیبان، اگر command line کار نکرد

## 📝 نکات مهم

⚠️ **Production**: در production، timeout را بیشتر از 60 ثانیه نگذارید. اگر کد شما بیش از 30 ثانیه طول می‌کشد، باید بهینه‌سازی شود.

✅ **Development**: برای development، timeout 300 ثانیه قابل قبول است.

## 🎯 نتیجه

بعد از این تغییرات:
- ✅ سرور با timeout 300 ثانیه اجرا می‌شود
- ✅ صفحه Dashboard باید بدون خطا لود شود
- ✅ Widget ها باید به درستی کار کنند

اگر بعد از این مراحل هنوز مشکل دارید، لطفاً لاگ‌ها و پیام خطای جدید را ارسال کنید.

