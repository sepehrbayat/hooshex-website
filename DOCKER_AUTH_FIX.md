# راه‌حل مشکل احراز هویت Docker

## 🔴 مشکل فعلی

```
Error response from daemon: authentication required - email must be verified before using account
```

Docker Desktop برای دانلود imageها از Docker Hub نیاز به لاگین دارد.

## ✅ راه‌حل‌ها

### روش 1: لاگین در Docker Desktop (پیشنهادی)

1. **Docker Desktop را باز کنید**
2. **به Settings بروید**:
   - روی آیکون تنظیمات (⚙️) در گوشه بالا سمت راست کلیک کنید
   - یا از منوی Docker Desktop: Settings
3. **در بخش "Docker Hub" یا "Account"**:
   - روی "Sign in" یا "Login" کلیک کنید
   - ایمیل و پسورد Docker Hub خود را وارد کنید
   - اگر اکانت ندارید، از [hub.docker.com](https://hub.docker.com) رایگان ثبت‌نام کنید
4. **پس از لاگین، دوباره تلاش کنید**:
   ```powershell
   docker compose up -d
   ```

---

### روش 2: استفاده از Docker Hub بدون لاگین (محدود)

Docker Hub برای کاربران غیر لاگین محدودیت دارد (100 pull در هر 6 ساعت). اما ممکن است کار کند:

1. **در Docker Desktop به Settings > Docker Hub بروید**
2. **گزینه "Use Hub anonymously" را فعال کنید** (اگر موجود باشد)
3. **یا مستقیماً دوباره تلاش کنید** - ممکن است کار کند!

---

### روش 3: تنظیم Docker Hub Credential

اگر لاگین کردید اما هنوز کار نمی‌کند:

```powershell
# لاگین از طریق command line
docker login
```

سپس:
- Username: ایمیل یا username شما در Docker Hub
- Password: پسورد Docker Hub
- یا Personal Access Token (از Docker Hub > Account Settings > Security)

---

## 🚀 پس از لاگین

پس از لاگین موفق، این دستورات را اجرا کنید:

```powershell
cd C:\Users\dell\Documents\hooshex-website

# راه‌اندازی سرویس‌ها
docker compose up -d

# بررسی وضعیت
docker compose ps

# مشاهده لاگ‌ها
docker compose logs -f
```

---

## 📝 نکات مهم

1. **Docker Hub Account رایگان است** - اگر ندارید، از [hub.docker.com](https://hub.docker.com) ثبت‌نام کنید
2. **لاگین یکبار انجام می‌شود** - پس از آن، Docker Desktop credentialها را ذخیره می‌کند
3. **Imageهای عمومی نیازی به لاگین ندارند** - اما Docker Desktop جدیدتر ممکن است لاگین اجباری کند

---

**تاریخ**: 2025-12-27

