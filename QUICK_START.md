# راهنمای سریع راه‌اندازی

## 🚀 راه‌اندازی یک کلیکی

### روش ساده (پیشنهادی):

```powershell
.\start-all.ps1
```

یا اگر PowerShell blocked است:

```cmd
start-servers.bat
```

---

## ✅ پس از راه‌اندازی

### بررسی وضعیت:

```powershell
# بررسی پورت‌ها
netstat -ano | findstr ":7668 :6365"

# بررسی Docker
docker compose ps
```

### URLهای مهم:

- **پنل کاربری**: http://127.0.0.1:7668/app/login
  - ایمیل: `user@test.com`
  - رمز: `password`

- **پنل ادمین**: http://127.0.0.1:7668/admin/login

---

## ⚠️ مشکلات رایج

### 1. Docker Desktop نیست
**راه‌حل**: Docker Desktop را باز کنید

### 2. پورت اشغال است
**راه‌حل**: 
```powershell
# پیدا کردن PID
netstat -ano | findstr ":7668"

# متوقف کردن
taskkill /PID <PID> /F
```

### 3. سرورها راه‌اندازی نمی‌شوند
**راه‌حل**: 
```powershell
# نصب وابستگی‌ها
npm install
composer install

# راه‌اندازی مجدد
.\start-all.ps1
```

---

**تاریخ**: 2025-12-27

