# راهنمای راه‌اندازی Docker

## 🔍 مشکل فعلی

Docker نصب شده است اما دستورات `docker` و `docker-compose` در PATH شناسایی نمی‌شوند.

## ✅ راه‌حل‌ها (به ترتیب اولویت):

### 1️⃣ راه‌اندازی Docker Desktop

**مهم**: Docker Desktop باید در حال اجرا باشد!

1. Docker Desktop را باز کنید:
   - از منوی Start، "Docker Desktop" را جستجو و اجرا کنید
   - یا از آیکون Desktop اجرا کنید

2. منتظر بمانید تا Docker Engine راه‌اندازی شود:
   - آیکون Docker در System Tray (کنار ساعت) باید سبز شود
   - این ممکن است 1-2 دقیقه طول بکشد

3. پس از راه‌اندازی کامل، PowerShell/CMD جدید باز کنید و دوباره تست کنید

---

### 2️⃣ Restart کردن PowerShell/CMD

پس از نصب Docker، PATH به‌روزرسانی می‌شود. ممکن است نیاز باشد:

1. تمام پنجره‌های PowerShell/CMD را ببندید
2. یک پنجره جدید باز کنید
3. دستورات Docker را دوباره تست کنید

---

### 3️⃣ دسترسی مستقیم به Docker

اگر Docker Desktop در حال اجرا است اما PATH کار نمی‌کند، می‌توانید از مسیر کامل استفاده کنید:

```powershell
# برای docker
& "C:\Program Files\Docker\Docker\resources\bin\docker.exe" --version

# برای docker-compose (نسخه جدید)
& "C:\Program Files\Docker\Docker\resources\bin\docker.exe" compose --version
```

---

### 4️⃣ بررسی PATH

برای بررسی اینکه Docker در PATH است:

```powershell
$env:PATH -split ';' | Select-String -Pattern "Docker"
```

---

## 🚀 پس از راه‌اندازی Docker Desktop

پس از اینکه Docker Desktop در حال اجرا شد و PATH درست کار کرد، این دستورات را اجرا کنید:

```powershell
# رفتن به پوشه پروژه
cd C:\Users\dell\Documents\hooshex-website

# بررسی نسخه Docker
docker --version
docker compose version

# راه‌اندازی سرویس‌ها
docker compose up -d

# بررسی وضعیت
docker compose ps

# مشاهده لاگ‌ها
docker compose logs -f
```

---

## ⚠️ نکات مهم

1. **Docker Desktop باید همیشه در حال اجرا باشد** - اگر ببندید، containerها متوقف می‌شوند
2. **اولین بار**: دانلود imageها ممکن است چند دقیقه طول بکشد
3. **پورت‌ها**: مطمئن شوید که پورت‌های 5432، 6379، و 7700 آزاد هستند

---

## 🔧 عیب‌یابی

### Docker Desktop باز نمی‌شود
- WSL 2 را نصب کنید: `wsl --install`
- Virtualization را در BIOS فعال کنید
- Hyper-V را فعال کنید

### پورت در حال استفاده است
```powershell
# بررسی استفاده از پورت
netstat -ano | findstr ":5432"
netstat -ano | findstr ":6379"
netstat -ano | findstr ":7700"
```

### Containerها راه‌اندازی نمی‌شوند
```powershell
# مشاهده لاگ‌های کامل
docker compose logs

# مشاهده لاگ یک سرویس خاص
docker compose logs postgres
docker compose logs redis
docker compose logs meilisearch
```

---

**تاریخ**: 2025-12-27

