# راهنمای راه‌اندازی سرورها

## 🚀 راه‌اندازی سریع

### روش 1: PowerShell (پیشنهادی)

```powershell
.\start-all.ps1
```

یا:

```powershell
powershell -ExecutionPolicy Bypass -File start-all.ps1
```

### روش 2: Batch File (Windows)

```cmd
start-servers.bat
```

### روش 3: npm (اگر Docker قبلاً راه‌اندازی شده)

```bash
npm run start
```

---

## 📋 چه چیزهایی راه‌اندازی می‌شوند؟

### 1. Docker Services
- ✅ PostgreSQL (پورت 5432)
- ✅ Redis (پورت 6379)
- ✅ Meilisearch (پورت 7700)

### 2. Application Servers
- ✅ Laravel Backend (پورت 7668)
- ✅ Vite Dev Server (پورت 6365)

---

## 🌐 URLهای مهم

| سرویس | URL |
|-------|-----|
| **Laravel** | http://127.0.0.1:7668 |
| **Vite** | http://127.0.0.1:6365 |
| **پنل کاربری** | http://127.0.0.1:7668/app/login |
| **پنل ادمین** | http://127.0.0.1:7668/admin/login |

---

## ⚙️ گزینه‌های اسکریپت

### start-all.ps1

```powershell
# رد کردن Docker (اگر قبلاً راه‌اندازی شده)
.\start-all.ps1 -SkipDocker
```

---

## 🔧 عیب‌یابی

### Docker Desktop در حال اجرا نیست

```
⚠️  Docker Desktop در حال اجرا نیست!
```

**راه‌حل**: Docker Desktop را باز کنید و منتظر بمانید تا کاملاً راه‌اندازی شود.

### پورت در حال استفاده است

```bash
# بررسی استفاده از پورت
netstat -ano | findstr ":7668"
netstat -ano | findstr ":6365"

# اگر پروسه‌ای در حال استفاده است، آن را متوقف کنید
taskkill /PID <PID> /F
```

### سرورها راه‌اندازی نمی‌شوند

1. بررسی کنید Node.js نصب است:
   ```bash
   node --version
   npm --version
   ```

2. وابستگی‌ها را نصب کنید:
   ```bash
   npm install
   composer install
   ```

3. Docker services را بررسی کنید:
   ```bash
   docker compose ps
   ```

---

## 🛑 توقف سرورها

### توقف Laravel و Vite
- در ترمینال: `Ctrl+C`

### توقف Docker Services
```bash
docker compose down
```

### توقف همه چیز
```bash
# توقف Docker
docker compose down

# توقف Node processes
taskkill /F /IM node.exe
```

---

## 📝 نکات مهم

1. **Docker Desktop باید همیشه در حال اجرا باشد** (برای Docker services)
2. **اولین بار**: دانلود imageها ممکن است چند دقیقه طول بکشد
3. **پورت‌ها**: مطمئن شوید که پورت‌های 7668 و 6365 آزاد هستند
4. **وابستگی‌ها**: اگر `node_modules` یا `vendor` وجود ندارد، اسکریپت به صورت خودکار نصب می‌کند

---

## 🔄 راه‌اندازی مجدد

برای راه‌اندازی مجدد کامل:

```powershell
# توقف همه چیز
docker compose down
taskkill /F /IM node.exe

# راه‌اندازی مجدد
.\start-all.ps1
```

---

**تاریخ**: 2025-12-27

